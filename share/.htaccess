# Share viewer: never index, never leak referrers, never cache
<IfModule mod_headers.c>
    Header set X-Robots-Tag "noindex, nofollow"
    Header set Referrer-Policy "no-referrer"
    Header set Cache-Control "no-store"
</IfModule>
